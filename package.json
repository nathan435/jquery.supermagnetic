{
  "name": "jquerysupermagnetic",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node server.js",
    "build": "babel src -d build --copy-files",
    "dev": "npm run server | babel src -d build --copy-files -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "express": "^4.14.0"
  }
}
